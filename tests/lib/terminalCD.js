var rewire = require('rewire')
  , terminalCD = rewire('../../lib/terminalCD')

describe('Should parse directory changes in the terminal', function(){

  it('Should open the terminal-cd log file', function(){


  })
})
