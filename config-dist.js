var config = {
  token: null,            // @todo what was this for?
  host: 'localhost',
  port: 30001,
  db: {
    host: 'localhost',
    port: '27017',
    dbName: 'taskTracker'
  }
}

module.exports = config
